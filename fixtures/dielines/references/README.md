# Dieline Reference Fixtures

Put authorized source-of-truth dielines here when exact template matching is required.

Supported names for the first exact target:

- `reverse-tuck-end.svg`
- `reverse-tuck-end.dxf`

The verification script will compare the generated Reverse Tuck End graph against `reverse-tuck-end.svg` when that file exists. Keep source files in millimeters, with stable layer names such as `cut`, `crease`, `perf`, `window`, `hole`, `bleed`, and `safe`.

